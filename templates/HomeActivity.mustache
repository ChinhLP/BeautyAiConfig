package {{packageName}}

import android.content.Intent
import android.os.Bundle
import vn.apero.vslbeautyfull.ui.VslBeautyFullHomeActivity

class HomeActivity : VslBeautyFullHomeActivity() {
     companion object{
            fun startFromSplash(context: Context){
                VslBeautyFullHomeActivity.startActivityAndPreloadAds(
                    context = context,
                    intent  = Intent(context, HomeActivity::class.java)
                )
            }
        }

}